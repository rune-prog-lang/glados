override
def show(x: i32, y: i32) -> null
{
    show("salut tu vas bien?\n");
    show(x);
    show('\n');
    show(y);
    show('\n');
}

def main() -> null
{
    a = 0;
    b = 3;

    show(a, b);
}
